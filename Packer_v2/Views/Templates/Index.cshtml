@model IEnumerable<Packer_v2.Models.Templates>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NmTemplate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NmTemplate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTemplate }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTemplate }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTemplate })
        </td>
    </tr>
}

</table>
